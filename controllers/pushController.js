/*
const express = require('express'); 
const app = express();

const PUSH = require('../models/push.js');

router.get('/', (req, res) => {
    res.render('index', {
      pushes: PUSH,
    })
  });

module.exports = router;
*/